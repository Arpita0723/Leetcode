class Solution {
public:
    bool isPalindrome(int x) {
        string s=to_string(x);
        for(int i=0;s[i]!='\0';i++){
            if(s[i]!=s[s.length()-1-i]){
                return false;
                break;
            }
        }
        return true;
    }
};
